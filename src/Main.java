public class Main {
    public static void main(String[] args) {
        /*
          TODO:
          1. Hacer clase cliente y servidor
          2. Hacer que el cliente le mande los numeros y la primera operación (suma) y comprobar estado de la misma
          3. Hacer que el cliente le mande las numeros y las operaciones (resta, multiplicación, división) y comprobar estados de la misma
          4. Si lo anterior funciona, añadir más operaciones algo más complejas (raíz cuadrada, porcentaje)
          5. Hacer que cada conexión del cliente con el servidor, se instancie en un hilo diferente
          6. Hacer que cuando nos devuelva el resultado el servidor, podamos operar con el mismo para mandar otra operación
          7. Si lo anterior funcional, haremos una interfaz sencilla de calculadora (esto es opcional)
         */
    }
}